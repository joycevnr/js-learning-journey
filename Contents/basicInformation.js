// JavaScript é uma linguagem case sensitive (isso significa que a linguagem vê diferença entre letras maiúsculas e minúsculas)

// Exemplo de declaração de variáveis
let variavel; // Declarar variável que pode ser modificada
const constante = 10; // Declarar constante cujo valor não pode ser modificado
var outraVariavel; // Declarar variável com var (evitar o uso)

// Exemplo de atribuição e saída de dados
let numero = 12;
console.log(numero); // Saída: 12

// Função do operador +
console.log("5" + "5"); // Saída: "55" (concatenação de strings)
console.log(5 + 5); // Saída: 10 (adição de números)

// Exemplo de uso do typeof
let variavelExemplo = 2;
console.log(typeof variavelExemplo); // Saída: number

// Comentário
/*.....
.....*/       //comentário de mais de uma linha

//  comentário de uma linha

// Um algoritmo é uma sequência de passos de maneira precisa, sem ambiguidade, com o objetivo de resolver um problema.


// Nomenclatura de variáveis:
// Snake case: usa underline (_) para separar palavras.
let snake_case;

// Camel case: primeira letra minúscula e a primeira letra de cada nova palavra subsequente em maiúscula.
let camelCase;

// Pascal case: todas as palavras começam com a primeira letra em maiúscula.
let PascalCase;